package OperateExcel;

import java.util.ArrayList;

public class ExcelUtil {
    public static void writeExcel(ArrayList<Students> list,String...header) {
        if (list == null || list.size() == 0||header==null) {
            return;
        }
        //1.创建Excel对象
        XSSFWorkbook =
    }
}
